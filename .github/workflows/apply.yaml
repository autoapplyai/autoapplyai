name: Auto-Apply AI

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'

jobs:
  apply:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install selenium pyyaml requests weasyprint jinja2

      - name: Install Google Chrome & matching ChromeDriver
        run: |
          # Install Chrome
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" \
            | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable unzip

          # Derive major version
          CHROME_VER=\$(google-chrome --version | awk '{print \$3}' | cut -d. -f1)
          echo "🔍 Detected Chrome major version \$CHROME_VER"

          # Fetch matching driver
          DRIVER_VER=\$(curl -sS "https://chromedriver.storage.googleapis.com/LATEST_RELEASE_\$CHROME_VER")
          echo "🔍 Fetching ChromeDriver version \$DRIVER_VER"
          wget -N "https://chromedriver.storage.googleapis.com/\$DRIVER_VER/chromedriver_linux64.zip" -P /tmp
          unzip -o /tmp/chromedriver_linux64.zip -d /tmp
          sudo mv /tmp/chromedriver /usr/local/bin/
          sudo chmod +x /usr/local/bin/chromedriver

      - name: Find jobs
        run: python find_jobs.py

      - name: Run Auto-Apply
        env:
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
        run: python generate_application.py
